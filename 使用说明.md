# 选完美图片赢礼物 · 使用说明

纯 HTML + CSS + JS 的移动端单页，适合在微信里分享打开。所有代码都在 `index.html`。

## 页面流程

1. **首页**：标题「选出你认为最完美的一张图片」，下方 4 张图片选项。任意一张都会进入下一页，没有错误反馈。
2. **过渡页**：显示「答对了！」和彩带/星星庆祝动画，约 1.5 秒后出现「查看我的礼物」。
3. **礼物页**：
   - 一封写给你的信（CSS 信封）
   - 礼物 A / 礼物 B（单选，再点可取消）
   - 底部祝福语

## 如何改文案

| 要改的内容 | 位置 |
|-----------|------|
| 首页标题 | `h1`：「选出你认为最完美的一张图片」 |
| 祝福语 | 脚本 `CONFIG.blessing` |
| 礼物名称 | 「礼物A」「礼物B」文字 |
| 按钮延迟 | `CONFIG.giftButtonDelay`（毫秒） |

## 如何换成真实图片

每个选项是白色卡片 + 彩色占位块。占位块上有注释说明。把 `.pic-ph` 里的文字 span 换成 `<img>` 即可，样式已写好铺满圆角框。

## 注意事项

- 不依赖外部图片、字体、框架，微信内置浏览器可直接打开。
- 适配宽度约 320px–428px。
- 点击有按压缩放反馈。
